-First Execute all queries of DB_Main. It created database and tables and also generates data for practise.

Note: All queries are written in MS-SQl. So when you run this query in other DBMS some command may not work.

